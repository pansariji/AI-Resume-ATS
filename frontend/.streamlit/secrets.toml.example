# Streamlit Secrets (optional if using .env locally, needed if deploying to Streamlit Cloud)

[supabase]
SUPABASE_URL = ""
SUPABASE_ANON_KEY = ""

[backend]
url = "http://localhost:8000"

[google_oauth]
redirect_uri = "http://localhost:8501"
